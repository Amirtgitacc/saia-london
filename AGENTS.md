# SAÏA London — agent instructions

**All project instructions live in [`CLAUDE.md`](CLAUDE.md) — read that file and follow it.**
It covers: run/verify commands, the two-tier concierge architecture, the single source of
truth (`js/saia-knowledge.js`), how to edit each layer, the delivery/courier pricing model,
the Shopify theme workflow, and the brand rules.

This file is only a pointer so non-Claude tools find the same instructions. Don't duplicate
content here — edit `CLAUDE.md` instead.
